// jest.setup.js
process.env.VITE_API_BASE_URL = "http://localhost:5000";

// Optional MSW setup if you're mocking network
// let server;

// beforeAll(() => {
//   if (server) server.listen();
// });

// afterEach(() => {
//   if (server) server.resetHandlers();
// });

// afterAll(() => {
//   if (server) server.close();
// });
